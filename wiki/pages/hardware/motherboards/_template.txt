===== @!PAGE@ =====

<WRAP box right 340px>
^ Board name ^^
^ Codename | Internal name(s), from the kernel's system tables or Digital's documents |
^ CPU | [[wp>Alpha_21264|21264]] (EV6), with part number and EV name for each processor offered |
^ Core logic | [[hardware:chipsets|chipset]] part number and name, e.g. 21272 (Tsunami) |
^ Memory | Type, slots, and maximum, e.g. sets of four DIMMs, up to 32 GiB |
^ Buses | e.g. 64-bit PCI, 32-bit PCI, AGP, ISA |
^ Form factor | e.g. ATX, Baby AT |
^ Onboard I/O | e.g. IDE, floppy, 2 serial, parallel, USB, Ethernet |
^ Firmware | SRM (last V//x.y//), AlphaBIOS, MILO, as available, from [[software:firmware:srm#final_versions|Final versions]] |
^ Kernel option | ''CONFIG_ALPHA_...'', from [[documentation:kernel#configuring_for_a_system|Kernel]] |
^ Linux | Supported / Untested / Linux 6.9 and earlier / Removed in //year// / Never |
</WRAP>

One or two sentences: what the board is, who made it, and when. Cite a source for each fact.

Delete any infobox row whose value has no source. The Linux row takes one of these values, which the [[hardware:motherboards|Motherboards]] table also uses:
  * **Supported**: current kernels support the board.
  * **Untested**: the support code is in current kernels, but no one has reported booting a current kernel, or there is a specific doubt, such as a board that only MILO can boot.
  * **Linux 6.9 and earlier**: the board has a processor older than the EV56 (21164A).
  * **Removed in //year//**: support was removed before Linux 6.10 for another reason.
  * **Never**: Linux never supported the board.

Use only the sections that have content, in this order. Sizes are in KiB, MiB, and GiB.

==== Specifications ====

Subsections (''=== Variant ==='') may separate board variants.

==== CPU ====

==== Memory ====

==== Cache ====

==== Power supply ====

Named "Power supply and cooling" when it also covers fans and heatsinks.

==== Onboard devices ====

==== Firmware ====

Which consoles the board has, the version to use (normally the final version), and how to update the flash.

==== Firmware recovery ====

==== Beep codes ====

==== Linux ====

Open with one sentence giving current kernel status, the kernel option (and whether a generic kernel works), and the boot path, for example: "Current kernels support the board with ''CONFIG_ALPHA_...'' or a generic kernel; it boots from SRM with aboot." For a board with a processor older than the EV56, use: "Linux 6.9 was the last version to support the //board//; Linux 6.10 removed support for processors older than the EV56 (21164A)." Then list the onboard devices with their drivers, and anything specific to Linux.

==== Known issues ====

Hardware limitations also go here; do not use a "Limitations" section.

==== Clones and OEM systems ====

==== Part numbers ====

==== History ====

==== Conflicting information ====

The following sources conflict with each other or with the account given above. They are listed here for further investigation.

==== Documentation ====

^ Document ^ Part number ^ Date ^

{{tag>hardware motherboard}}
